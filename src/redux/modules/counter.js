// 초기 상태 값
const initialState = {
  number: 0,
};

// 리듀서
const counter = (state = initialState, action) => {
  switch (action.type) {
    case value:
    default:
      return state;
  }
};

export default counter;
